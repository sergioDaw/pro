package cap1;

public class eje1_10 {

	public static void main(String[] args) {
		
		int edad=30, nivel_estudios=2, ingresos=0;
		boolean jasp=false;
		
		if ((edad <= 28) && (nivel_estudios > 3) || (edad <30) && (ingresos>28000));
		jasp = true;
		
		//Tengo la duda en este, debemos inicializar la variable jasp asi o es mejor otro metodo?

	}

}
